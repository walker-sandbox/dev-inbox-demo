# dev-inbox-demo
Demo repo for the mermail-dev-inbox skill (GitHub notification triage)

## Purpose

This repository exists to generate real GitHub notification email (failed CI runs, Dependabot alerts, review requests, releases) into a Mermail mailbox for the `mermail-dev-inbox` agent skill demo.
